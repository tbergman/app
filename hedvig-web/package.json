{
  "name": "hedvig-web",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "history": "^4.6.3",
    "normalize.css": "^7.0.0",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-redux": "^5.0.6",
    "react-router-dom": "^4.1.2",
    "react-router-redux": "next",
    "react-scripts": "1.0.11",
    "redux": "^3.7.2",
    "styled-components": "^2.1.2"
  },
  "scripts": {
    "start": "make link && react-scripts start",
    "build": "make prepare-build && react-scripts build && make link",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn build && s3cmd sync build/* s3://hedinternal"
  }
}
