language: node_js

sudo: false

node_js:
  - "7"
  - "iojs"

# cache:
#   yarn: true
#   directories:
#     - node_modules

before_install:
  - npm install exp --global
  - exp login -u $EXPO_USERNAME -p $EXPO_PASSWORD

install:
  - cd hedvig-redux
  - yarn install
  - cd ..
  - cd hedvig-app
  - yarn install
  - yarn deploy

script:
  - echo "Done"
